export interface RecentActivity {
    id: number;
    title: string;
    organization: string;
    time: string;
    date: Date;
}

export interface Achievement {
    id: number;
    title: string;
    icon: string;
}