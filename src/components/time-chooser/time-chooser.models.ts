export interface TimeOption {
    name: string;
    hours: string;
    selected: boolean;
}

export interface DayOption {
    name: string;
    days: string;
    selected: boolean;
}

export interface OftenOption {
    name: string;
    frequency: string;
    selected: boolean;
}